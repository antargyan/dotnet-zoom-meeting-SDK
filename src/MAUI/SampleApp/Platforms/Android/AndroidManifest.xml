<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" tools:replace="android:allowBackup,android:label"></application>
  <!-- minSdkVersion comes from $(SupportedOSPlatformVersion) in the csproj (28 - mobilertc.aar
       declares minSdkVersion=28). Setting it here as well fails the build with XA1036. -->
  <uses-sdk android:targetSdkVersion="36" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
</manifest>